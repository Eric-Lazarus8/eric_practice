#include<stdio.h>
int main()
{
	int i;
	printf("enter hex value\n");
	scanf("%x",&i);
	int value1,value2,bit1,bit2;
	printf("Enter the values of value1 and bit1\n ");
	scanf("%d %d",&value1,&bit1);
	printf("Enter the value of value2 and bit2\n");
	scanf("%d %d",&value2,&bit2);
	int swap;
	swap|=((value1<<bit1)|(value2<<bit2));
	printf("swap of 2 niblis:%x\n",swap);
	reset=((value <<bit1)|(va);
	printf

}
